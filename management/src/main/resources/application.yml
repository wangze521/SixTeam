server:
  port: 8088

dubbo:
  application:
    name: dubbox-management
  protocol:
    host: localhost
    name: dubbo
    port: 20880
  registry:
    address: localhost:2181
    protocol: zookeeper

thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        max-idle: 500
        min-idle: 0
    lettuce:
      shutdown-timeout: 0


